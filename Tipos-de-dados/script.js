// Declare uma variável contendo uma string
var filme = 'Titanic';

// Declare uma variável contendo um número dentro de uma string
var valor = '2';

// Declare uma variável com a sua idade
var minhaIdade = 33;

// Declare duas variáveis, uma com seu nome
var meuNome = 'Simone';

// e outra com seu sobrenome e some as mesmas
var meuSobrenome = 'Silva';
var nomeCompleto = meuNome + ' ' + meuSobrenome;
var nomeCompleto2 = `${meuNome} ${meuSobrenome}`;

// Coloque a seguinte frase em uma variável: It's time
var fraseVar = 'It\'s time';
var fraseVar2 = `It's time`;
var fraseVar3 = "It's time";

// Verifique o tipo da variável que contém o seu nome
var verificarTipoNome = typeof meuNome;

console.log(verificarTipoNome);



console.log(filme,valor, minhaIdade, meuNome, meuSobrenome, nomeCompleto, fraseVar, fraseVar2, fraseVar3  );
